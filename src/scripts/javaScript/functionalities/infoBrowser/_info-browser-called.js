/**
 *  Llamados
 *
 ==============================================================================*/
window.addEventListener("resize", () => {
  //detectBrowser();
  //mobileDetecting();
  //viewportSize();
});


/* =============================================================================*/

(() => {
  //detectBrowser();
  // mobileDetecting();
  //viewportSize();

  if ($('body').is('.mobile')) {

  }

})();
